# A generated secret for Payload (generate with: openssl rand -hex 32)
PAYLOAD_SECRET=

# Public URL of this site (used for sitemap, OG, RSS, canonical URLs)
NEXT_PUBLIC_SITE_URL=https://news.ginnytech.it

# Optional: log level for Payload in production
PAYLOAD_LOG_LEVEL=info
